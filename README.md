# AdaptiveResolutionISMs

## Prerequisites
This project uses CMake as its build system. Please make sure you have CMake version 3.10 or newer and then run the GENERATE_SLN.bat file. This will generate a Visual Studio solution in the build folder.